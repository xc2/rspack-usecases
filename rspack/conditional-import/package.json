{
  "private": true,
  "scripts": {
    "build": "rm -rf dist && rspack build",
    "wp": "rm -rf dist && webpack --config rspack.config.js"
  },
  "dependencies": {
    "@rspack/cli": "^0.4.3",
    "@rspack/core": "^0.4.3",
    "@wasm-fmt/ruff_fmt": "^0.5.0",
    "webpack": "5"
  }
}
