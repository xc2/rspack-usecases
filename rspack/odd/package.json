{
  "private": true,
  "scripts": {
    "build": "rspack build",
    "wp": "webpack --config rspack.config.js",
    "all": "rspack build && webpack --config rspack.config.js"
  },
  "dependencies": {
    "@rspack/cli": "nightly",
    "@rspack/core": "nightly",
    "webpack": "5"
  }
}
